Sticky white page header with a hairline base. `left` holds the Breadcrumb, `right` holds account links/actions.

```jsx
<TopBar
  left={<Breadcrumb items={[{label:"Workers",href:"#"}, "Jose Hernandez"]} />}
  right={<><a href="#">Contact</a><a href="#">Sign out</a></>}
/>
```
