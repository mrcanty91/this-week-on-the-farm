Single-line text field — 40px, hairline border, blue focus ring.

```jsx
<Input placeholder="Full name" />
<Input invalid prefix={<SearchIcon/>} />
```

`invalid` for errors; `prefix`/`suffix` for adornments. Pair with `FormField`.
