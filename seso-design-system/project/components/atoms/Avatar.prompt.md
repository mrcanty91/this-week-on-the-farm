Worker / user image with initials fallback.

```jsx
<Avatar name="Jose Hernandez" src={photo} shape="rounded" />
<Avatar name="Carlos F" size={28} />
```

`shape`: `circle` (users) · `rounded` (worker photos) · `square`.
