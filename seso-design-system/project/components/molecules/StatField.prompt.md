Labeled metadata cell — the worker header strip (Type / Status / ID / DOB / Crew).

```jsx
<StatField label="Type"><Badge tone="green">H-2A</Badge></StatField>
<StatField label="Employee ID" mono>18095</StatField>
```
