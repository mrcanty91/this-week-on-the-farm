{
  "name": "farm-weather-advisor",
  "version": "0.1.0",
  "private": true,
  "description": "This Week on the Farm — weather-backed crop & crew calls for CONUS farm operators.",
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\"",
    "check": "for f in src/*.js; do node --check \"$f\" || exit 1; done"
  },
  "engines": {
    "node": ">=22"
  }
}
